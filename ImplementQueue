l=[]
for _ in range(int(input())):
    s=input()
    if s[0]=="E":
        l.append(s[8:])
    else:
        if len(l)>=1:
            n=l.pop(0)
            print(n)
        else:
            print("Empty")
    
    
